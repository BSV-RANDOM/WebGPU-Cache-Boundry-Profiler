# WebGPU-Cache-Boundry-Profiler
Simple single html file tool for testing GPU bandwidth at different payload sizes.

When payload grows larger than GPU Cache, bandwidth falls significantly as system offloads to global memory.
